SET(CHANNEL_LIDAR_SRC
  ChannelLidar.cpp
  )

ADD_LIBRARY(channel_lidar STATIC ${CHANNEL_LIDAR_SRC})

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/channel_lidar )
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/common )
