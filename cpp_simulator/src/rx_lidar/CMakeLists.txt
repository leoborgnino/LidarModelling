SET(RX_LIDAR_SRC
  RxLidarPulsed.cpp
  RxLidarFMCW.cpp
  )

ADD_LIBRARY(rx_lidar STATIC ${RX_LIDAR_SRC})

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/rx_lidar )
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/common )
